pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'cd Docker'
                sh 'mvn -B -DskipTests clean package' 
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
    }
}
